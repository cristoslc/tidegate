# Tidegate configuration — local development
# Points to localhost. Use root tidegate.yaml for Docker Compose.

version: "1"

defaults:
  scan_timeout_ms: 500
  scan_failure_mode: deny

servers:
  echo:
    transport: http
    url: http://localhost:4200/mcp
